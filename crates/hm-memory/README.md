# hm-memory

Persistente MemoryStore mit semantischer Vektor-Suche + strukturellem Knowledge-Graph.

## Aufruf (Beispiel)

```bash
cargo test -p hm-memory
```

## Quelle

- `crates/hm-memory/src/`
- `crates/hm-memory/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
