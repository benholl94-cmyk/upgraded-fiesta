# hm-tool-browser

CDP-Browser-Steuerung (Chrome DevTools Protocol).

## Wire-Identitaet

- Plugin/task_type-Name: `browser`
- Public-API-Stabilitaet: semver folgt `Cargo.toml` (`workspace.package.version` = `0.1.0`).

## Aufruf (Beispiel)

```bash
cargo test -p hm-tool-browser
```

## Quelle

- `crates/hm-tools/hm-tool-browser/src/`
- `crates/hm-tools/hm-tool-browser/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
