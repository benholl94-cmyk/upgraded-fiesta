# hm-tool-media

Media-Bearbeitungs-Plugin (Platzhalter, noch kein echter Codec).

## Wire-Identitaet

- Plugin/task_type-Name: `media`
- Public-API-Stabilitaet: semver folgt `Cargo.toml` (`workspace.package.version` = `0.1.0`).

## Aufruf (Beispiel)

```bash
cargo test -p hm-tool-media
```

## Quelle

- `crates/hm-tools/hm-tool-media/src/`
- `crates/hm-tools/hm-tool-media/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
