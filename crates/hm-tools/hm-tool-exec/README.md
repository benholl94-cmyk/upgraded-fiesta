# hm-tool-exec

Subprocess-Plugin `ops-tool`: read-only Checks (disk/memory/gateway-status) ueber fixed allowlist.

## Wire-Identitaet

- Plugin/task_type-Name: `ops-tool`
- Public-API-Stabilitaet: semver folgt `Cargo.toml` (`workspace.package.version` = `0.1.0`).

## Aufruf (Beispiel)

```bash
cargo test -p hm-tool-exec
```

## Quelle

- `crates/hm-tools/hm-tool-exec/src/`
- `crates/hm-tools/hm-tool-exec/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
