# hm-channel-whatsapp

Outbound: Meta Graph API send_message (HTTPS, opt-in `tls`-Feature).

## Aufruf (Beispiel)

```bash
cargo test -p hm-channel-whatsapp
```

## Quelle

- `crates/hm-channels/hm-channel-whatsapp/src/`
- `crates/hm-channels/hm-channel-whatsapp/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
