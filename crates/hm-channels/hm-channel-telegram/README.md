# hm-channel-telegram

Outbound: Telegram sendMessage (HTTPS, opt-in `tls`-Feature).

## Aufruf (Beispiel)

```bash
cargo test -p hm-channel-telegram
```

## Quelle

- `crates/hm-channels/hm-channel-telegram/src/`
- `crates/hm-channels/hm-channel-telegram/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
