# hm-core

Gemeinsame Datentypen und PluginRequest/PluginResponse-Vertrag fuer alle Crates.

## Aufruf (Beispiel)

```bash
cargo test -p hm-core
```

## Quelle

- `crates/hm-core/src/`
- `crates/hm-core/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
