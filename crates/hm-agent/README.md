# hm-agent

TaskDispatch zu hm-plugins; verknuepft Gateway-Eingang mit Plugin-Aufruf und Memory-Eintrag.

## Aufruf (Beispiel)

```bash
cargo test -p hm-agent
```

## Quelle

- `crates/hm-agent/src/`
- `crates/hm-agent/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
