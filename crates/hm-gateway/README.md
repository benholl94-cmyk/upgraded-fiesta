# hm-gateway

Hand-rolled Async-TCP-HTTP-Server; einziger Auth- und Routing-Punkt fuer das ganze System.

## Aufruf (Beispiel)

```bash
cargo test -p hm-gateway
```

## Quelle

- `crates/hm-gateway/src/`
- `crates/hm-gateway/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
