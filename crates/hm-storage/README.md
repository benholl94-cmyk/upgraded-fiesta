# hm-storage

FileStorage-Trait mit zwei Backends: LocalFsStorage (Platte) und RemoteHttpStorage (fremdes Gateway).

## Aufruf (Beispiel)

```bash
cargo test -p hm-storage
```

## Quelle

- `crates/hm-storage/src/`
- `crates/hm-storage/Cargo.toml`

_Dieses README wird durch `scripts/generate_crate_readmes.py` erzeugt._
_Manuelle Edits werden beim naechsten Lauf nicht ueberschrieben (Schutzmechanismus)._
