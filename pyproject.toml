[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ghm-core"
version = "0.1.0"
description = "Fullstack Heavy Metal mobile workspace core CLI"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
ghm-core = "ghm_core.cli:main"

[tool.setuptools]
packages = ["ghm_core"]
