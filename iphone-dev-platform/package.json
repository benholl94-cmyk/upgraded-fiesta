{
  "name": "iphone-dev-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Static German iPhone developer platform with safe Direct-Inject setup helpers.",
  "scripts": {
    "init": "python3 scripts/validate.py",
    "test": "python3 scripts/validate.py && node --check app.js && node --check service-worker.js && node scripts/test-app.js",
    "serve": "python3 -m http.server 8000"
  }
}
